CREATE TABLE IF NOT EXISTS games (
  id SERIAL PRIMARY KEY,
  titulo TEXT NOT NULL,
  ano INTEGER NOT NULL CHECK (ano > 1950 AND ano <= EXTRACT(YEAR FROM CURRENT_DATE)),
  genero TEXT NOT NULL,
  publisher TEXT NOT NULL
);
